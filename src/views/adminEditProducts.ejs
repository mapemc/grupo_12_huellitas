<!DOCTYPE html>
<html lang = "es">
<head>
    <%- include("./partials/head.ejs", {cssSheet:"stylesAdminEditProducts", title:"Formulario de creación"}) %>
</head>

<body>
    <header>
        <%- include('partials/header.ejs') %>
    </header>

    <main>
        <h3 class = "title">Editar Producto</h3>
        <form class="form" action = "/products/edit/<%= productToEdit.name %>" method = "POST" class = "products-form" enctype="multipart/form-data">
        <!--Cuerpo-->
            <div class = "name-description">
                <label for = "name" class = "form-label">Nombre</label>
                    <input type = "text" name = "name" id = "name" class = "form-input" value="<%= productToEdit.name %>">
                <label for = "description">Descripción</label>
                    <textarea name="description" id="description" class="form-input"><%= productToEdit.description %></textarea>
            </div>

            <div class="image">
                <label for="image" class="form-label">Fotos</label>
                <div class="thumbnail-container">
                    <% if (productToEdit.image && productToEdit.image.length > 0) { %>
                        <% productToEdit.image.forEach(function(image) { %>
                            <div class="thumbnail-item">
                                <img src="/img/<%= image %>">
                            </div>
                        <% }); %>
                    <% } %>
                </div>
                <input type="file" name="image" multiple accept=".jpg, .png, .svg" id="image" class="form-input">
            </div>            

            <div class = "price-stock">
            <label for = "price" class = "form-label">Precio</label>
                <input type = "number" step = 0.01 min = 0 name = "price" id = "price" class = "form-input" value="<%= productToEdit.price %>">
            <label for = "stock" class = "form-label">Stock</label>
                <input type = "number" min = 0 name = "stock" id = "stock" class = "form-input" value="<%= productToEdit.stock %>">
            </div>

            <div class="category">
                <label for="category" class="form-label">Categoría</label>
                <select name="category" id="category" class="form-input">
                    <option value="default-cat" <% if (productToEdit.category === 'default-cat') { %> selected <% } %>>Categoría</option>
                    <option value="accesories" <% if (productToEdit.category === 'accesories') { %> selected <% } %>>Accesorios</option>
                    <option value="food" <% if (productToEdit.category === 'food') { %> selected <% } %>>Alimento</option>
                    <option value="sale" <% if (productToEdit.category === 'sale') { %> selected <% } %>>Oferta</option>
                </select>
            </div>

            <div class = "specs">
                <label for="color" class="form-label">Color</label>
                    <select name="color" id="color" class="form-input">
                        <option value="default-color" <% if (productToEdit.color === 'default-color') { %> selected <% } %>>Color</option>
                        <option value="yellow" <% if (productToEdit.color === 'yellow') { %> selected <% } %>>Amarillo</option>
                        <option value="blue" <% if (productToEdit.color === 'blue') { %> selected <% } %>>Azul</option>
                        <option value="black" <% if (productToEdit.color === 'black') { %> selected <% } %>>Negro</option>
                        <option value="red" <% if (productToEdit.color === 'red') { %> selected <% } %>>Rojo</option>
                        <option value="green" <% if (productToEdit.color === 'green') { %> selected <% } %>>Verde</option>
                    </select>

                <label for = "product-size" class = "form-label" value = "<%= productToEdit.color %>">Tamaño</label>
                    <div class = "size-checkboxes">
                        <label for = "sizeS"><input type = "checkbox" id = "sizeS" name = "sizes" value = "S">S</label>
                        <label for = "sizeM"><input type = "checkbox" id = "sizeM" name = "sizes" value = "M">M</label>
                        <label for = "sizeL"><input type = "checkbox" id = "sizeL" name = "sizes" value = "L">L</label>
                    
                    </div>
            </div>
            <div class = "button-container">
                <!--Boton borrar producto-->
                <button type = "submit" id = "deleteButton" class = "deleteButton">Eliminar</button>
                <!--Boton borrar contenido y volver para atras-->
                <a href = "/products/products" class = "cancelButton">Cancelar</a>
                <!--Boton editar aplicar cambios-->
                <button type = "submit" id = "editProductButton" class = "editProductButton">Aplicar Cambios</button>
            </div>
        </form>
    </main>

    <footer>
        <%- include('partials/footer.ejs') %>
    </footer>
</body>	
</html>